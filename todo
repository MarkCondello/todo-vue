New data structure for Google Keep clone
        /* todo: {
            id: int
            title: 'str',
            type: {
                list: boolean,
                checklist: boolean,
            },
            pinned: boolean,
            archived: boolean,
            colour: 'str',
            edited: dateTime,
            order: number,
            opened: boolean,
            selected: boolean,
            labels: [],
            images: [],
            tasks: [
                {
                    id: int
                    order: int,
                    name: 'str',
                    checklist_props : {
                        is_parent: boolean (false if nesting level 2 or 3),
                        is_sub_parent: boolean (false if nesting level 3 or 1),
                        is_sub_sub_parent: boolean (false if nesting level 2 or 1),
                        nesting_level: 1, 2 or 3,
                        completed: boolean (for checklist items only),
                    },
                }
            ],
        }*/